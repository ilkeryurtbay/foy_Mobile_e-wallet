import '../../../../../../../../../../../../global/export/export.dart';

class QrNfcPage extends StatelessWidget {
  const QrNfcPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: const [
          Center(),
        ],
      ),
    );
  }
}
